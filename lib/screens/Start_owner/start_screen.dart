import 'package:flutter/material.dart';
import 'package:project_application/screens/Start_owner/components/body.dart';

class StartOwnerScreen extends StatelessWidget {
  const StartOwnerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(child: Column(),),
    );
  }
}
