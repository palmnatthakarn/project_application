import 'package:flutter/material.dart';
import 'components/body.dart';
class ProfileOnwerScreen extends StatelessWidget {
  const ProfileOnwerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ProfileScreen(),
    );
  }
}
