import 'package:flutter/material.dart';

class StartUserScreen extends StatelessWidget {
  const StartUserScreen({Key? key}) : super(key: key);

  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Start User"),

      ),  
    );
  }   
} 

